(function () {
    angular.module('app', []);      // app is the name of the module
})();                               // [] we would put in the brackets any directives that need to be added 
                                    // from outside sources such as the google one for mapping 